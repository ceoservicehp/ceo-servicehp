"use strict";const c=window.supabaseClient,$=x=>document.getElementById(x),rp=n=>"Rp "+Number(n||0).toLocaleString("id-ID"),esc=v=>String(v??"").replace(/[&<>"']/g,x=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[x]));const L={menunggu_diproses:"Menunggu",diproses:"Diproses",dikemas:"Dikemas",dikirim:"Dikirim",dalam_perjalanan:"Dalam Perjalanan",selesai:"Selesai",dibatalkan:"Batal",belum_bayar:"Belum Bayar",sebagian:"Sebagian",lunas:"Lunas",pickup:"Ambil di Toko",instant:"Kurir Instan",package:"Kirim Paket",cod:"COD Lokal"},lab=v=>L[v]||String(v||"-").replaceAll("_"," ");document.addEventListener("DOMContentLoaded",()=>{const q=new URLSearchParams(location.search).get("order");if(q)$("orderNumber").value=q;$("checkBtn").onclick=check});async function check(){const no=$("orderNumber").value.trim(),ph=$("phone").value.trim();if(!no||!ph)return msg("Nomor pesanan dan WhatsApp wajib diisi.");const b=$("checkBtn");b.disabled=true;try{const{data,error}=await c.rpc("public_track_product_order",{p_order_number:no,p_customer_whatsapp:ph});if(error)throw error;if(!data?.success)throw Error(data?.message||"Pesanan tidak ditemukan.");render(data)}catch(e){msg(e.message)}finally{b.disabled=false}}function msg(t){$("alert").hidden=false;$("alert").textContent=t;$("result").hidden=true}function render(d){$("alert").hidden=true;const o=d.order||{},s=d.shipping||{},w=d.warranties||[],it=d.items||[],steps=["menunggu_diproses","diproses","dikemas","dikirim","dalam_perjalanan","selesai"],cur=steps.indexOf(o.order_status);$("result").innerHTML=`<div class="head"><div><small>NO. PESANAN</small><h2>${esc(o.order_number)}</h2></div><b>${esc(lab(o.order_status))}</b></div><div class="timeline">${steps.map((x,i)=>`<div class="${i<=cur?"on":""}"><span>${i<cur?'<i class="fa-solid fa-check"></i>':i+1}</span><small>${lab(x)}</small></div>`).join("")}</div><div class="cards"><article><h3>Produk</h3>${it.map(x=>`<p><span>${esc(x.product_name)} × ${x.quantity}</span><b>${rp(x.unit_price*x.quantity)}</b></p>`).join("")}</article><article><h3>Pembayaran</h3><p><span>Total</span><b>${rp(o.total)}</b></p><p><span>Sudah Dibayar</span><b>${rp(o.amount_paid)}</b></p><p><span>Sisa</span><b>${rp(o.remaining_amount)}</b></p><p><span>Status</span><b>${lab(o.payment_status)}</b></p></article><article><h3>Pengiriman</h3><p><span>Metode</span><b>${lab(o.shipping_method)}</b></p><p><span>Kurir</span><b>${esc(s.courier||o.shipping_provider||"-")}</b></p><p><span>Resi</span><b>${esc(s.tracking_number||"-")}</b></p></article><article><h3>Garansi</h3>${w.length?w.map(x=>`<div class="war"><b>${esc(x.warranty_name||x.warranty_type||"Garansi")}</b><small>${esc(x.start_date||"-")} s/d ${esc(x.end_date||"-")}</small></div>`).join(""):'<span class="muted">Garansi belum diatur admin.</span>'}<div class="claim">Klaim garansi: simpan invoice dan sertakan nomor pesanan, penjelasan kendala, serta foto/video kondisi produk saat menghubungi CEO Part & Service.</div></article></div>`;$("result").hidden=false;$("result").scrollIntoView({behavior:"smooth"})}