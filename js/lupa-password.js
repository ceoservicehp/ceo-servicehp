"use strict";
const client=window.supabaseClient, form=document.getElementById("forgotForm"), alertBox=document.getElementById("alertBox");
function msg(text,type="error"){alertBox.style.display="block";alertBox.className=`alert-box ${type==="success"?"alert-success":"alert-error"}`;alertBox.textContent=text;}
form?.addEventListener("submit",async e=>{e.preventDefault();if(!client){msg("Supabase belum siap.");return;}const email=document.getElementById("resetEmail").value.trim();const btn=form.querySelector("button");btn.disabled=true;btn.innerHTML='<i class="fa-solid fa-circle-notch fa-spin"></i> Mengirim...';const {error}=await client.auth.resetPasswordForEmail(email,{redirectTo:window.location.origin+"/reset-password.html"});btn.disabled=false;btn.innerHTML='<i class="fa-solid fa-paper-plane"></i> Kirim Link Reset';if(error){msg(error.message);return;}msg("Jika email terdaftar, link reset password telah dikirim. Silakan cek Inbox atau Spam.","success");});
