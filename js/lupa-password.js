document.addEventListener('DOMContentLoaded',()=>{const client=window.supabaseClient,form=document.getElementById('forgotForm'),msg=document.getElementById('message'),btn=document.getElementById('submitBtn');const show=(t,type)=>{msg.hidden=false;msg.className='message '+type;msg.textContent=t};form.addEventListener('submit',async e=>{e.preventDefault();const email=document.getElementById('email').value.trim();btn.disabled=true;const old=btn.innerHTML;btn.innerHTML='<i class="fa-solid fa-spinner fa-spin"></i> Mengirim...';const {error}=await client.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password.html`});btn.disabled=false;btn.innerHTML=old;if(error)return show('Gagal mengirim link reset: '+error.message,'error');show('Jika email terdaftar, link reset password telah dikirim. Silakan cek inbox atau folder spam.','success')})});
